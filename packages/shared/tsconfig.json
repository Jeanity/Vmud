{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Platform-free by design: no DOM, no Node. If something here needs either, it belongs in
    // another package.
    "lib": ["ES2022"],
    "types": ["node"]
  },
  "include": ["src/**/*.ts"]
}
