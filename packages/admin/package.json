{
  "name": "@mygame/admin",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "The operator's panel. A client of the game server's admin API; owns no game state.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "vite": "^6.0.7"
  }
}
